# .husky/pre-commit

#!/bin/sh
. "$(dirname -- "$0")/_/husky.sh"

yarn lint

if [ $? -ne 0 ]; then
  echo "🚨 Linting failed. Please fix the issues and try again."
  exit 1
else
  echo "✅ Linting passed!"
fi
